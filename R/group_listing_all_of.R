# TileDB Storage Platform API
#
# TileDB Storage Platform REST API
#
# The version of the OpenAPI document: 2.2.19
# 
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title GroupListingAllOf
#'
#' @description GroupListingAllOf Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field groups  list( \link{Group} ) [optional]
#'
#' @field assets  list( \link{ArrayInfo} ) [optional]
#'
#' @field pagination_metadata  \link{PaginationMetadata} [optional]
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
GroupListingAllOf <- R6::R6Class(
  'GroupListingAllOf',
  public = list(
    `groups` = NULL,
    `assets` = NULL,
    `pagination_metadata` = NULL,
    initialize = function(
        `groups`=NULL, `assets`=NULL, `pagination_metadata`=NULL, ...
    ) {
      local.optional.var <- list(...)
      if (!is.null(`groups`)) {
        stopifnot(is.vector(`groups`), length(`groups`) != 0)
        sapply(`groups`, function(x) stopifnot(R6::is.R6(x)))
        self$`groups` <- `groups`
      }
      if (!is.null(`assets`)) {
        stopifnot(is.vector(`assets`), length(`assets`) != 0)
        sapply(`assets`, function(x) stopifnot(R6::is.R6(x)))
        self$`assets` <- `assets`
      }
      if (!is.null(`pagination_metadata`)) {
        stopifnot(R6::is.R6(`pagination_metadata`))
        self$`pagination_metadata` <- `pagination_metadata`
      }
    },
    toJSON = function() {
      GroupListingAllOfObject <- list()
      if (!is.null(self$`groups`)) {
        GroupListingAllOfObject[['groups']] <-
          lapply(self$`groups`, function(x) x$toJSON())
      }
      if (!is.null(self$`assets`)) {
        GroupListingAllOfObject[['assets']] <-
          lapply(self$`assets`, function(x) x$toJSON())
      }
      if (!is.null(self$`pagination_metadata`)) {
        GroupListingAllOfObject[['pagination_metadata']] <-
          self$`pagination_metadata`$toJSON()
      }

      GroupListingAllOfObject
    },
    fromJSON = function(GroupListingAllOfJson) {
      GroupListingAllOfObject <- jsonlite::fromJSON(GroupListingAllOfJson)
      if (!is.null(GroupListingAllOfObject$`groups`)) {
        self$`groups` <- ApiClient$new()$deserializeObj(GroupListingAllOfObject$`groups`, "array[Group]", loadNamespace("tiledbcloud"))
      }
      if (!is.null(GroupListingAllOfObject$`assets`)) {
        self$`assets` <- ApiClient$new()$deserializeObj(GroupListingAllOfObject$`assets`, "array[ArrayInfo]", loadNamespace("tiledbcloud"))
      }
      if (!is.null(GroupListingAllOfObject$`pagination_metadata`)) {
        pagination_metadataObject <- PaginationMetadata$new()
        pagination_metadataObject$fromJSON(jsonlite::toJSON(GroupListingAllOfObject$pagination_metadata, auto_unbox = TRUE, digits = NA))
        self$`pagination_metadata` <- pagination_metadataObject
      }
      self
    },
    toJSONString = function() {
      jsoncontent <- c(
        if (!is.null(self$`groups`)) {
        sprintf(
        '"groups":
        [%s]
',
        paste(sapply(self$`groups`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox=TRUE, digits = NA)), collapse=",")
        )},
        if (!is.null(self$`assets`)) {
        sprintf(
        '"assets":
        [%s]
',
        paste(sapply(self$`assets`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox=TRUE, digits = NA)), collapse=",")
        )},
        if (!is.null(self$`pagination_metadata`)) {
        sprintf(
        '"pagination_metadata":
        %s
        ',
        jsonlite::toJSON(self$`pagination_metadata`$toJSON(), auto_unbox=TRUE, digits = NA)
        )}
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste('{', jsoncontent, '}', sep = "")
    },
    fromJSONString = function(GroupListingAllOfJson) {
      GroupListingAllOfObject <- jsonlite::fromJSON(GroupListingAllOfJson)
      self$`groups` <- ApiClient$new()$deserializeObj(GroupListingAllOfObject$`groups`, "array[Group]", loadNamespace("tiledbcloud"))
      self$`assets` <- ApiClient$new()$deserializeObj(GroupListingAllOfObject$`assets`, "array[ArrayInfo]", loadNamespace("tiledbcloud"))
      self$`pagination_metadata` <- PaginationMetadata$new()$fromJSON(jsonlite::toJSON(GroupListingAllOfObject$pagination_metadata, auto_unbox = TRUE, digits = NA))
      self
    }
  )
)

