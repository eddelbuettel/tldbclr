# TileDB Storage Platform API
#
# TileDB Storage Platform REST API
#
# The version of the OpenAPI document: 2.2.19
# 
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title SubarrayRanges
#'
#' @description SubarrayRanges Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field type  \link{Datatype} [optional]
#'
#' @field hasDefaultRange  character [optional]
#'
#' @field buffer  list( integer ) [optional]
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
SubarrayRanges <- R6::R6Class(
  'SubarrayRanges',
  public = list(
    `type` = NULL,
    `hasDefaultRange` = NULL,
    `buffer` = NULL,
    initialize = function(
        `type`=NULL, `hasDefaultRange`=NULL, `buffer`=NULL, ...
    ) {
      local.optional.var <- list(...)
      if (!is.null(`type`)) {
        stopifnot(R6::is.R6(`type`))
        self$`type` <- `type`
      }
      if (!is.null(`hasDefaultRange`)) {
        self$`hasDefaultRange` <- `hasDefaultRange`
      }
      if (!is.null(`buffer`)) {
        stopifnot(is.vector(`buffer`), length(`buffer`) != 0)
        sapply(`buffer`, function(x) stopifnot(is.character(x)))
        self$`buffer` <- `buffer`
      }
    },
    toJSON = function() {
      SubarrayRangesObject <- list()
      if (!is.null(self$`type`)) {
        SubarrayRangesObject[['type']] <-
          self$`type`$toJSON()
      }
      if (!is.null(self$`hasDefaultRange`)) {
        SubarrayRangesObject[['hasDefaultRange']] <-
          self$`hasDefaultRange`
      }
      if (!is.null(self$`buffer`)) {
        SubarrayRangesObject[['buffer']] <-
          self$`buffer`
      }

      SubarrayRangesObject
    },
    fromJSON = function(SubarrayRangesJson) {
      SubarrayRangesObject <- jsonlite::fromJSON(SubarrayRangesJson)
      if (!is.null(SubarrayRangesObject$`type`)) {
        # MANUAL EDIT AFTER OPENAPI AUTOGEN
        # For enums, OpenAPI autogen (1) generates a constructor which requires being called
        # with one arguent; (2) generates callsites (such as here) that calls that constructor
        # with zero arguments.
        #
        #typeObject <- Datatype$new()
        #typeObject$fromJSON(jsonlite::toJSON(SubarrayRangesObject$type, auto_unbox = TRUE, digits = NA))
        typeObject <- Datatype$new(SubarrayRangesObject$type)
        self$`type` <- typeObject
      }
      if (!is.null(SubarrayRangesObject$`hasDefaultRange`)) {
        self$`hasDefaultRange` <- SubarrayRangesObject$`hasDefaultRange`
      }
      if (!is.null(SubarrayRangesObject$`buffer`)) {
        self$`buffer` <- ApiClient$new()$deserializeObj(SubarrayRangesObject$`buffer`, "array[integer]", loadNamespace("tiledbcloud"))
      }
      self
    },
    toJSONString = function() {
      jsoncontent <- c(
        if (!is.null(self$`type`)) {
        sprintf(
        '"type":
        %s
        ',
        jsonlite::toJSON(self$`type`$toJSON(), auto_unbox=TRUE, digits = NA)
        )},
        if (!is.null(self$`hasDefaultRange`)) {
        sprintf(
        '"hasDefaultRange":
          "%s"
                ',
        self$`hasDefaultRange`
        )},
        if (!is.null(self$`buffer`)) {
        sprintf(
        '"buffer":
           [%s]
        ',
        paste(unlist(lapply(self$`buffer`, function(x) paste0('"', x, '"'))), collapse=",")
        )}
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste('{', jsoncontent, '}', sep = "")
    },
    fromJSONString = function(SubarrayRangesJson) {
      SubarrayRangesObject <- jsonlite::fromJSON(SubarrayRangesJson)
      self$`type` <- Datatype$new()$fromJSON(jsonlite::toJSON(SubarrayRangesObject$type, auto_unbox = TRUE, digits = NA))
      self$`hasDefaultRange` <- SubarrayRangesObject$`hasDefaultRange`
      self$`buffer` <- ApiClient$new()$deserializeObj(SubarrayRangesObject$`buffer`, "array[integer]", loadNamespace("tiledbcloud"))
      self
    }
  )
)

