# TileDB Storage Platform API
#
# TileDB Storage Platform REST API
#
# The version of the OpenAPI document: 2.2.19
# 
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title GroupSharingRequest
#'
#' @description GroupSharingRequest Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field group_actions  list( \link{GroupActions} ) [optional]
#'
#' @field array_actions  list( \link{ArrayActions} ) [optional]
#'
#' @field namespace  character [optional]
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
GroupSharingRequest <- R6::R6Class(
  'GroupSharingRequest',
  public = list(
    `group_actions` = NULL,
    `array_actions` = NULL,
    `namespace` = NULL,
    initialize = function(
        `group_actions`=NULL, `array_actions`=NULL, `namespace`=NULL, ...
    ) {
      local.optional.var <- list(...)
      if (!is.null(`group_actions`)) {
        stopifnot(is.vector(`group_actions`), length(`group_actions`) != 0)
        sapply(`group_actions`, function(x) stopifnot(R6::is.R6(x)))
        self$`group_actions` <- `group_actions`
      }
      if (!is.null(`array_actions`)) {
        stopifnot(is.vector(`array_actions`), length(`array_actions`) != 0)
        sapply(`array_actions`, function(x) stopifnot(R6::is.R6(x)))
        self$`array_actions` <- `array_actions`
      }
      if (!is.null(`namespace`)) {
        stopifnot(is.character(`namespace`), length(`namespace`) == 1)
        self$`namespace` <- `namespace`
      }
    },
    toJSON = function() {
      GroupSharingRequestObject <- list()
      if (!is.null(self$`group_actions`)) {
        GroupSharingRequestObject[['group_actions']] <-
          lapply(self$`group_actions`, function(x) x$toJSON())
      }
      if (!is.null(self$`array_actions`)) {
        GroupSharingRequestObject[['array_actions']] <-
          lapply(self$`array_actions`, function(x) x$toJSON())
      }
      if (!is.null(self$`namespace`)) {
        GroupSharingRequestObject[['namespace']] <-
          self$`namespace`
      }

      GroupSharingRequestObject
    },
    fromJSON = function(GroupSharingRequestJson) {
      GroupSharingRequestObject <- jsonlite::fromJSON(GroupSharingRequestJson)
      if (!is.null(GroupSharingRequestObject$`group_actions`)) {
        self$`group_actions` <- ApiClient$new()$deserializeObj(GroupSharingRequestObject$`group_actions`, "array[GroupActions]", loadNamespace("tiledbcloud"))
      }
      if (!is.null(GroupSharingRequestObject$`array_actions`)) {
        self$`array_actions` <- ApiClient$new()$deserializeObj(GroupSharingRequestObject$`array_actions`, "array[ArrayActions]", loadNamespace("tiledbcloud"))
      }
      if (!is.null(GroupSharingRequestObject$`namespace`)) {
        self$`namespace` <- GroupSharingRequestObject$`namespace`
      }
      self
    },
    toJSONString = function() {
      jsoncontent <- c(
        if (!is.null(self$`group_actions`)) {
        sprintf(
        '"group_actions":
        [%s]
',
        paste(sapply(self$`group_actions`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox=TRUE, digits = NA)), collapse=",")
        )},
        if (!is.null(self$`array_actions`)) {
        sprintf(
        '"array_actions":
        [%s]
',
        paste(sapply(self$`array_actions`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox=TRUE, digits = NA)), collapse=",")
        )},
        if (!is.null(self$`namespace`)) {
        sprintf(
        '"namespace":
          "%s"
                ',
        self$`namespace`
        )}
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste('{', jsoncontent, '}', sep = "")
    },
    fromJSONString = function(GroupSharingRequestJson) {
      GroupSharingRequestObject <- jsonlite::fromJSON(GroupSharingRequestJson)
      self$`group_actions` <- ApiClient$new()$deserializeObj(GroupSharingRequestObject$`group_actions`, "array[GroupActions]", loadNamespace("tiledbcloud"))
      self$`array_actions` <- ApiClient$new()$deserializeObj(GroupSharingRequestObject$`array_actions`, "array[ArrayActions]", loadNamespace("tiledbcloud"))
      self$`namespace` <- GroupSharingRequestObject$`namespace`
      self
    }
  )
)

